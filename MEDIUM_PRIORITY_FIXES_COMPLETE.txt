# M-1 to M-6 Fixes Complete

## Summary

All medium priority fixes have been implemented:

| Fix | Status | Details |
|-----|--------|---------|
| M-1 | ✅ | AtomicLong increment removed - hash-only ID generation |
| M-2 | ✅ | Exception context preservation implemented |
| M-3 | ✅ | HTTP 408 added to retryable codes |
| M-4 | ✅ | LRU cache with size limit implemented |
| M-5 | ✅ | Explicit when statements replacing reflection |
| M-6 | ✅ | VRAM estimation improved with context window & batch size |

## Changes Made

### M-6: LocalLlamaAdapter.kt - VRAM Estimation

**File:** `provider-adapters/src/main/kotlin/com/shadowai/provideradapters/LocalLlamaAdapter.kt`

**Before:**
```kotlin
fun estimateVramRequirement(fileSizeBytes: Long): Int {
    val sizeMB = fileSizeBytes / (1024 * 1024)
    return (sizeMB * VRAM_MULTIPLIER).toInt()
}
```

**After:**
```kotlin
fun estimateVramRequirement(
    fileSizeBytes: Long,
    nCtx: Int = 2048,
    batchSize: Int = 1
): Int {
    val sizeMB = fileSizeBytes / (1024 * 1024)
    
    // Base VRAM for model weights and overhead
    val baseVramMB = (sizeMB * VRAM_MULTIPLIER).toInt()
    
    // Context window VRAM: nCtx * 2 bytes per token * 2x factor
    val contextVramMB = (nCtx * 2L * 2) / (1024 * 1024)
    
    // Batch size VRAM: additional buffer for each batch
    val batchVramMB = (batchSize * nCtx * 2L) / (1024 * 1024)
    
    return baseVramMB + contextVramMB.toInt() + batchVramMB.toInt()
}
```

## Performance Impact

- **M-1 to M-5:** Minimal or positive impact (better algorithms, bounded cache)
- **M-6:** Negligible - estimation only runs during initialization

All fixes verified and ready for merge.